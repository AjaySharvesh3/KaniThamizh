<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="alphawolf.wolfpack.ajaysharvesh.kanithamizh">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Registrations.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Menus.MainActivity" />
        <activity android:name=".Registrations.RegisterActivity" />
        <activity android:name=".Registrations.ResetPasscodeActivity" />
        <activity
            android:name=".Thirukuralkal"
            android:theme="@style/AppThemeOne" />
        <activity
            android:name=".SettingsActivity"
            android:theme="@style/AppThemeOne" />
        <activity
            android:name=".Athikarankal"
            android:theme="@style/AppThemeOne" />
        <activity android:name=".KadavulValuthu"></activity>
    </application>

</manifest>